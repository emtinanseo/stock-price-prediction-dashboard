import pandas as pd

def arima_predict(data: pd.DataFrame):
    # logic for using ARIMA
    return [0]*len(data)

def regression_predict(data: pd.DataFrame):
    # logic for using ARIMA
    return [1]*len(data)